{{- if not .Values.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "terraria.fullname" . }}
  labels:
    {{- include "terraria.fullname" . | nindent 4 }}
type: Opaque
data:
  SERVER_PASSWORD: {{ .Values.serverPassword | b64enc | quote }}
{{- end }}
